# Sessione del 2026-09-15 — chiusura difetti audit

Seguito di `sessione-2026-09-14.md`.

**`flutter analyze` passato pulito il 2026-09-15** ("No issues found!") sulle
modifiche di entrambe le sessioni, dopo `run_dev.bat gen-l10n` (le quattro
chiavi ARB nuove vanno rigenerate: i file in `lib/app/l10n/generated/` sono
versionati).

---

## Difetti chiusi

| ID | Cosa sembrava | Cosa era davvero | Correzione |
|---|---|---|---|
| **A-11** | Meteo pista datato 2-4 aprile | `_mockWeatherForTrack` inventava tre giorni con date fisse e verdetti tipo "oggi si corre", mostrati ogni volta che Open-Meteo non rispondeva o la pista non aveva coordinate | Funzione eliminata. Senza previsioni reali la scheda dice "Previsioni non disponibili al momento" |
| **A-12** | Chip Admin inerti, sembrano tab | Sei chip con i nomi delle sezioni, non cliccabili, che duplicavano l'elenco sottostante | Rimossi. Restano i due contatori (coda, feedback), che informano invece di fingere di navigare |
| **A-14** | Pista creata da Gestione non collegata all'account | Il collegamento in `track_managers` avveniva solo al momento dell'approvazione. I **negozi** facevano già la cosa giusta (trigger AFTER INSERT): le due entità erano incoerenti | Trigger AFTER INSERT anche per le piste + riallineamento delle esistenti |
| **A-15** | Cover URL valida ignorata, mai richiesta in rete | `fetchPublicTracks` e `fetchManagedTracks` non selezionavano `image_url`, quindi `TrackListItem.imageUrl` era sempre null. **Stessa famiglia di B-03** | Colonna aggiunta a entrambe le query |
| **A-17** | Home mostra i dati dell'admin durante l'impersonazione | Il saluto usava `userProfileProvider` invece di `effectiveUserProfileProvider` | Cambiato provider. Rimosso anche il ripiego sulla parte locale dell'email |
| **A-18** | Tile "Categorie pista" non si aggiorna | La lista veniva invalidata, la tile legge `adminOverviewProvider` che non veniva invalidato. Lo stesso valeva per Piste, Negozi, Eventi dopo un'eliminazione | Invalidazione aggiunta in tutti e sei i punti |
| **A-20** | Admin Eventi promette controllo visibilità, offre solo il cestino | Il toggle esiste solo per la tabella `events`; tutti gli eventi reali stanno in `community_events`, che non ha la colonna `visibility` | Testo corretto per dire chi può fare cosa |
| **A-23** | Categorie "Francobolli" e "Treni" fuori dominio | Confermato: sono **categorie di pista**, non interessi. Nessuna delle due è collegata a piste | **Decisione tua** — SQL pronto in `qa-temp/cleanup-prod-2026-09-13.md` |
| **A-24** | `authError=otp_expired` con sessione valida | L'errore nel link veniva gestito **prima** di guardare se c'era già una sessione. Su web il fragment sopravvive alle navigazioni interne, quindi si ripresentava a ogni ricaricamento | Con sessione valida l'errore viene ignorato e il redirect richiesto viene comunque eseguito |
| **A-26** | "Vicino a te" promette eventi, non ne mostra mai | La schermata non ha nessun codice sugli eventi: era la descrizione a promettere troppo | Testo corretto (IT + EN). Gli eventi hanno già la loro sezione |
| **A-27** | Notifiche al destinatario sbagliato | I trigger erano **corretti**. Mancava `reviewed_by`: il client scriveva solo `approval_status`, quindi la guardia "non notificare chi ha deciso da solo" non scattava mai e l'autore della notifica restava sconosciuto | Trigger BEFORE UPDATE che timbra revisore e data a database, non nel client |

---

## Delta SQL

Entrambi **applicati su dev e verificati**, da eseguire su **prod**:

| File | Cosa fa |
|---|---|
| `2026-09-15-timbro-revisore.sql` | `reviewed_by` / `reviewed_at` timbrati a database su tracks e shops |
| `2026-09-15-pista-collegata-al-creatore.sql` | Collegamento gestore alla creazione della pista + riallineamento |

Più quello di ieri, ancora da applicare su prod:

| File | Cosa fa |
|---|---|
| `2026-09-14-identita-utente.sql` | Nome utente da Google + fallback neutro, `complete_onboarding` accetta il nome |

### Verifiche fatte su dev

- approvazione di una scheda altrui → `reviewed_by` = admin, notifica al proprietario, `created_by` valorizzato
- approvazione di una scheda propria → **nessuna** notifica creata
- pista inserita in stato `pending` con `submitted_by` → collegamento in `track_managers` presente subito
- dati di prova rimossi, stato della pista di collaudo ripristinato

---

## Cosa serve da te

1. ~~`analyze`~~ — fatto, pulito. Resta `run_dev.bat test`.
2. Eseguire i **tre delta** su prod.
3. Decidere su `Francobolli` / `Treni` (SQL pronto nelle note di pulizia).

## Cosa resta aperto

- 240 chiavi ARB non più usate (diff grosso, nessun impatto utente).
- Email nella lista utenti admin: servirebbe una RPC admin-only su `auth.users`. Ora che ogni profilo ha un nome, serve molto meno.
- Lo streak PitCoin in Home non segue l'impersonazione: l'RPC `get_my_pitcoin_streak` lavora su `auth.uid()`, che l'impersonazione UI-only non cambia. È un limite noto del meccanismo, non un difetto della Home.
